{
    "name": "diem-contracts",
    "version": "1.0.0",
    "description": "Monad smart contracts for Diem virtual world",
    "scripts": {
        "compile": "hardhat compile",
        "test": "hardhat test",
        "deploy:mainnet": "hardhat run scripts/deploy.ts --network monad-mainnet",
        "verify": "hardhat verify --network monad-mainnet"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^4.0.0",
        "@openzeppelin/contracts": "^5.0.1",
        "@typechain/hardhat": "^9.1.0",
        "ethers": "^6.10.0",
        "hardhat": "^2.19.4",
        "typechain": "^8.3.2"
    },
    "dependencies": {
        "dotenv": "^17.2.3"
    }
}